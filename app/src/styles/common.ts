import { StyleSheet } from "react-native";

// 画面ごとのエラーメッセージ表示で共通の見た目。
export const commonStyles = StyleSheet.create({
  error: {
    color: "#c0392b",
    fontSize: 14,
    textAlign: "center",
  },
});
